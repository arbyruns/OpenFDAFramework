import XCTest
@testable import OpenFDAFramework

final class OpenFDAFrameworkTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(OpenFDAFramework().text, "Hello, World!")
    }
}
